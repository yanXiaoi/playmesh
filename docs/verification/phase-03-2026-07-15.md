# 第三阶段验证记录（2026-07-15）

> 浏览器刷新复用旧玩家凭证是本记录当天的历史行为，已由 `phase-03-bugfix-2026-07-16.md` 的“刷新创建新玩家 ID”契约替代。

## 范围

本记录保存第三阶段收尾时的代码检查、自动测试和平台构建结果。功能事实见 `docs/status/phase-03-game-sdk-fishing.md`，游戏作者文档见 `docs/game/README.md`。

## 自动检查

| 命令 | 结果 |
|---|---|
| `dart format lib test` | 通过，56 个文件检查，0 个改动 |
| `flutter analyze` | 通过，`No issues found` |
| `flutter test` | 通过，共 39 条测试 |
| `node tool/test_game_sdk.mjs` | 通过，SDK 桥接、同步响应、生命周期、显式 FPS 和 Bucket 校验 |
| `node tool/test_game_sdk_browser.mjs` | 通过，浏览器刷新复用本局凭证并把 Bucket 写入主机端点 |
| `node tool/test_fishing_service.mjs` | 通过，全员准备、5 秒倒计时、答题、回合结束和第二回合自动推进 |
| `go test ./...` | 通过，health、server、session、mobile 包通过 |

覆盖重点：

- `main.json` 扫描、目录名一致性、方向、显示模式和 Authority 入口。
- 当前游戏 `app/` 与平台 `public/` 可访问，`data/` 和越界资源返回 404。
- `/join/{code}` 身份页、控制器、SDK 资源、主机存储接口和错误 token。
- 大屏公共显示端不进入 `players`，Authority 可以向全部玩家发送权威结果。
- Bucket 写入、重载、清除和路径边界；点号、空白、中文、前导下划线/连字符、超长名称均被拒绝。
- 游戏库、详情、开始、可拖动/收纳工具坞、FPS 开关、返回、退出、重新开始和屏幕方向恢复。

## Android 构建

```powershell
flutter build apk --debug
```

结果：通过。

- APK：`build/app/outputs/flutter-apk/app-debug.apk`
- 大小：184,694,625 字节
- SHA-256：`651567519D2ACA8D1694A38AB4BCE22D4554A852FDAB6699914BFE198DDD9ED2`
- 包含 `arm64-v8a`、`armeabi-v7a`、`x86_64` 三个 ABI 的 `libgojni.so`。
- 包含 `assets/playmesh-library/public/sdk/v1/playmesh.js`。
- SDK 产物包含显式 `performance.reportFrame()` 和浏览器主机存储端点调用，不包含旧 `playmesh.data.*` 浏览器 Bucket。
- 包含钓鱼 Demo 的 `main.json`、`app/index.html`、`app/controller/index.html` 和 `app/static/...`，未发现旧 `assets/sdk` 路径。
- Demo Authority 产物包含 `READY_COUNTDOWN_SECONDS = 5` 和 `player.ready`，不再包含 `round.next`。
- Android AAR：`android/app/libs/playmesh_core.aar`，21,001,114 字节。

## Windows 验证边界

按当前项目约定，Windows 构建和实际运行由用户自行验证。本轮自动收尾不执行 `flutter build windows`，也不以 Windows Runner 结果作为自动测试通过条件；用户确认的结果可随后追加到本记录。

## 未执行项

- 未使用 Android 真机做 WebView 手工验收。
- Windows Runner 和 WebView2 实机渲染由用户负责验证。
