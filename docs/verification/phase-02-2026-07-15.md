# 第二阶段验证记录（2026-07-15）

## 范围

本记录只保存第二阶段收尾的构建、测试和运行检查结果。功能与代码事实见 `docs/status/phase-02-go-core.md`。

## 自动检查

| 命令 | 结果 |
|---|---|
| `dart format lib test` | 通过，32 个文件检查，0 个改动 |
| `flutter analyze` | 通过，`No issues found` |
| `flutter test` | 通过，共 23 条测试 |
| `go test ./...` | 通过，health、server、mobile 包通过 |

覆盖重点：

- Core Client 成功、服务错误、无效 JSON、离线和超时。
- 协议版本和 `requestId` 校验。
- 宿主先启动再请求、启动失败映射和动态 endpoint 上报。
- 设置页在线、离线、错误和刷新。
- 游戏库到详情、开始、返回、退出和重新开始。
- WebView 在屏幕方向切换完成后创建，GamePage 销毁后恢复方向。
- `orientation` 接受 `landscape`/`portrait` 并拒绝未知值。

## Go Core 依赖构建

设置文档记录的 Android SDK 环境变量后执行：

```powershell
tool\build_go_core.ps1 -Target all
```

结果：通过。

- Android AAR：`android/app/libs/playmesh_core.aar`，19,768,181 字节。
- Windows Core：`build/go-core/windows/playmesh-core.exe`，8,900,096 字节。

首次未注入 `ANDROID_HOME` 时脚本按预期停止并报告环境未配置；注入 `D:\KaiFaTool\runtime\android-sdk` 后通过。

## 动态端口运行检查

启动新生成的 Windows Core，参数为 `-addr 127.0.0.1:0`，读取首条 `core.started` 结构化日志后请求 `/health`。

结果：

```text
Address:   127.0.0.1:58722
Port:      58722
Type:      core.health
RequestId: req-dynamic-port-verification
Status:    online
```

验证完成后立即停止进程。该端口只代表本次运行，不能作为配置保存。

## Android 构建

```powershell
flutter build apk --debug
```

结果：通过。

- APK：`build/app/outputs/flutter-apk/app-debug.apk`，182,113,049 字节。
- APK 包含 `arm64-v8a`、`armeabi-v7a`、`x86_64` 三个 ABI 的 `libgojni.so`。
- 未使用 Android 模拟器进行当前阶段运行判断。

## Windows 构建

```powershell
flutter build windows --debug --config-only
```

结果：通过。项目固定的 NuGet 7.6.0 路径生效，CMake 配置不再输出 `Nuget is not installed.`；WebView2 和 WIL 包已恢复到构建目录。

```powershell
flutter build windows --debug
```

结果：未完成。构建越过 CMake、NuGet 恢复、Flutter assets 生成和 Go Core 生成后，MSVC `cl.exe` 在首个原生编译进程持续零 CPU 且无输出；两次复现后终止相关 `cl`、MSBuild 和 CMake 进程。不能把已有旧 Runner 文件或中间目录视为本次完整构建通过。

当前结论：NuGet 缺失提示已经由项目 CMake 修正；完整 Windows Runner 仍受本机 MSVC 编译器停滞影响。Windows WebView2 的实际像素渲染尚无本次构建产物可验证。
