// Kept as the historical entry point for local scripts. The lifecycle is no
// longer online -> softSolo -> reconnecting: leaving the GDevelop lobby is now
// a local façade release over an App-owned, continuously attached session.
await import('./test_gdevelop_multiplayer_soft_leave.mjs');
