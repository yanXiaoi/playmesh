name: mobile_scanner
description: OpenHarmony Public SDK compatibility surface for Playmesh.
version: 6.0.2
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
